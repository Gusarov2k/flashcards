<%= simple_form_for( @pack, html: { class: 'form-group' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.button :submit, t('simple_form.pack.common.cr_pack', action: @pack.new_record? ? 'Создать' : 'Изменить'), class: "btn-primary" %>
<% end %>
